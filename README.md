# Actividad 4: Desarrollo Full Stack

# Gestor de Tareas y Productos - Express

Este proyecto es una aplicación backend desarrollada con Node.js y Express que se ejecuta en entorno local (localhost) y además la nueva añadición el despliegue en la nube de Render. Permite autenticación de usuarios, gestión de tareas y productos.

---

## Requisitos

Antes de ejecutar el proyecto, asegúrese de tener instalado:

- Node.js
- npm


## Ejecución del proyecto en localhost

### 1. Abrir Command Prompt

En Windows, abra **Command Prompt** (Símbolo del sistema).

---

### 2. Ubicarse en la carpeta del proyecto

Use el comando `cd` hasta llegar a la carpeta donde se encuentra el archivo `server.js` (o `index.js`):

```bash
cd ruta/del/proyecto
```
---
### 3. Instalar dependencias (solo la primera vez)

Ejecute el siguiente comando:

```bash
npm install
```

### 4. Iniciar el servidor

Ejecute el servidor con el siguiente comando:

```bash
node server.js
```

Si el servidor se inicia correctamente, aparecerá el mensaje:

```bash
Servidor Audi corriendo en http://localhost:3000
```

Acceso desde el navegador

Abra un navegador web y diríjase a la siguiente dirección:

```bash
http://localhost:3000
```

Esto cargará la página principal ubicada en la carpeta public.

---

## Ejecución del proyecto en Cloud

### 1. Abrir el siguiente enlace: https://actividad4-dfs.onrender.com/
 
Espere de 30 a 60 segundos y la página automáticamente se mostrará

---
### Credenciales de prueba

Para iniciar sesión desde la página web, utilice las siguientes credenciales:

```bash
Usuario: usuario1

Contraseña: 123
```



